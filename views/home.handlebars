{{> header}}

<h1>Welcome to the Tech Blog!</h1>
<div>
  {{#each posts as |post|}}
    <div>
      <h2>{{post.title}}</h2>
      <p>{{post.content}}</p> 
      <!-- more post details as needed -->
    </div>
  {{/each}}
</div>

{{> footer}} 
