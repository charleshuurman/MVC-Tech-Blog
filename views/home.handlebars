<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{pageTitle}}</title>
    <link rel="stylesheet" href="/css/homeStyles.css">


</head>

{{> header}}

<main class="main-content">
  <div class="posts-container">
    {{#each posts as |post|}}
      <div class="post">
        <!-- Top section for title and metadata -->
        <div class="post-header">
          <h2 class="post-title">{{post.title}}</h2>
          <p class="post-meta">Posted by {{post.author.username}} on {{formatDate post.createdAt}}</p>
        </div>

        <!-- Bottom section for content, image, and URL -->
        <div class="post-body">
          {{#if post.imageUrl}}
            <img src="{{post.imageUrl}}" alt="Post Image" class="post-image">
          {{/if}}
          <p class="post-content">{{post.content}}</p>
          {{#if post.url}}
            <p class="post-url">Read more at: <a href="{{post.url}}" target="_blank">{{post.url}}</a></p>
          {{/if}}
        </div>
      </div>
    {{/each}}
  </div>
</main>
{{> footer}}
